[project]
name = "cse_console"
authors = [{ name = "Combat Sports Education", email = "platform@combatsportseducation.com" }]
description = "Operator console + federation setup wizard for the CSE platform."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "cse_console"
